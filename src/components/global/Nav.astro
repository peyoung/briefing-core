---

---

<div class="globalNav">
  <nav>
    <ul>
      <li>
        <a href="/feature/briefing_cup_2025/">
          <span class="labelTx">All</span>
        </a>
      </li>
      <li>
        <a href="/feature/briefing_cup_2025/players/">
          <span class="labelTx">Players</span>
        </a>
      </li>
      <li>
        <a href="/feature/briefing_cup_2025/team/">
          <span class="labelTx">TEAM</span>
        </a>
      </li>
      <li>
        <a href="/feature/briefing_cup_2025/preparation/">
          <span class="labelTx">PREPARATION</span>
        </a>
      </li>
      <li>
        <a href="/feature/briefing_cup_2025/movie/">
          <span class="labelTx">MOVIE</span>
        </a>
      </li>
    </ul>
  </nav>
</div>
<!-- /.globalNav -->

<style lang="scss">
  @use 'sass:math';
  @use 'src/styles/global' as *;
  @use 'src/styles/utils' as *;
  ////////////////////
  .globalNav {
    position: fixed;
    bottom: get_sp(20);
    right: get_sp(20);
    text-align: right;
    @include fz_vw(26);
    line-height: math.div(20, 14) + em;
    text-transform: uppercase;
    transition: 0.3s;
    opacity: 0;
    transform: translateX(10em);
    // mix-blend-mode: difference;
    :global(.is-foot-disable) & {
      opacity: 1;
      transform: translateX(0em);
    }
    a {
      color: #5f5f5f;
      display: inline-block;
      cursor: default;
      pointer-events: none;
      :global(.is-foot-disable) & {
        cursor: pointer;
        pointer-events: all;
      }
      &.current {
        cursor: default;
        pointer-events: none;
        color: $white;
      }
      @media (any-hover: hover) {
        &:hover {
          transform: translateX(2px);
          color: $white;
        }
      }
    }
    .labelTx {
      font-weight: 600;
    }
  }
  @media screen and (min-width: $break-point-middle) {
    .globalNav {
      bottom: 44px;
      right: 30px;
      @include fz_vwpcmin(30);
      // font-size: 1.4rem;
    }
  }
  ////////////////////
  @media screen and (min-width: $break-point-middle) {
  }
</style>
