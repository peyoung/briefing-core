---
import { Image } from 'astro:assets';
import Logo from '@images/logo-briefing_01.svg';
---

<header class="g-mrgStyle_01">
  <div class="inner">
    <div class="col-logo">
      <a href="#g-wrapper">
        <Image src={Logo} alt="BRIEFING" width={144} densities={[1, 2, 3]} loading="eager" />
      </a>
    </div>
    <!-- /.col-logo -->
    <div class="col-nav">
      <div class="pickMenu">
        <ul>
          <li>
            <a href="#brandCore">
              <span class="labelTx">BRAND CORE</span>
              <!-- /.labelTx -->
            </a>
          </li>
          <li>
            <span class="labelTx" id="currentTx">TOP</span>
            <!-- /.labelTx -->
          </li>
        </ul>
      </div>
      <!-- /.pickMenu -->
      <div class="menuBtn">
        <button type="button" id="menuBtn">
          <span class="labelTx">MENU</span>
        </button>
      </div>
      <!-- /.menuBtn -->
    </div>
    <!-- /.col-nav -->
  </div>
  <!-- /.inner -->
</header>

<style lang="scss">
  @use 'sass:math';
  @use 'src/styles/global' as *;
  @use 'src/styles/utils' as *;
  ////////////////////
  header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    z-index: 1000;
    pointer-events: none;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.4s;
    @media screen and (max-width: $break-point-middle-sp) {
      align-items: flex-start;
      padding-top: get_sp($grid_01 - 40);
      padding-left: get_sp(20);
      padding-right: get_sp(20);
    }
    &.is-active {
      opacity: 1;
      :global(a),
      :global(button) {
        pointer-events: all;
      }
    }
    :global(.is-bottomView) & {
      opacity: 0;
      pointer-events: none;
    }
    > .inner {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: opacity 0.3s;
      :global(.menuOpen) & {
        opacity: 0;
        pointer-events: none;
      }
    }
  }
  ////////////////////
  .col-logo {
    img {
      @include imgset;
      width: 144px;
    }
  }
  @media screen and (max-width: $break-point-middle-sp) {
    .col-logo {
      display: none;
    }
  }
  ////////////////////
  .col-nav {
    display: flex;
    width: 100%;
    justify-content: space-between;
    // gap: math.div(139, 12) + em;
    line-height: normal;
    letter-spacing: math.div(-0.24, 12) + em;
    text-transform: uppercase;
    @include fz_vw(14);
    span {
      font-weight: 700;
    }
    a,
    button {
      color: $white;
      display: flex;
      gap: 0.5em;
      span {
        display: block;
      }
      @media (any-hover: hover) {
        &:hover {
          overflow: 0.9;
          transform: translateX(0.2em);
        }
      }
    }
  }
  @media screen and (min-width: $break-point-middle) {
    .col-nav {
      width: math.div(280, $design-num) * 100%;
      min-width: 280px;
      @include fz_vwpcmin(12);
    }
  }
  ////////////////////
  .pickMenu {
    ul {
      display: flex;
      gap: math.div(49, 12) + em;
      @media screen and (max-width: $break-point-middle-sp) {
        gap: math.div(14, 12) + em;
        transform: translateY(math.div(2, 12) + em);
      }
    }
    a {
      position: relative;
      &::before {
        content: '';
        height: 1px;
        width: 0%;
        left: 0;
        bottom: 0.2em;
        position: absolute;
        transition: 0.3s;
        background-color: $white;
      }
      @media (any-hover: hover) {
        &:hover {
          &::before {
            width: 100%;
          }
        }
      }
    }
  }
  ////////////////////
  .menuBtn {
    button {
      @include btnsetting;
      position: relative;
      overflow: hidden;
      transition: 0.3s;
      &::before {
        content: '';
        height: 1px;
        width: 100%;
        background-color: $white;
        bottom: 0.2em;
        position: absolute;
        transform: translateX(0);
        transition: 0.3s;
      }
      @media (any-hover: hover) {
        &:hover {
          &::before {
            background-color: $key-color1;
            animation: linemove_01 0.8s cubic-bezier(0.58, 0.3, 0.005, 1) 0s 1;
          }
        }
      }
    }
    // .is-normal {
    //   :global(.menuOpen) & {
    //     display: none;
    //   }
    // }
    // .is-open {
    //   display: none;
    //   :global(.menuOpen) & {
    //     display: block;
    //   }
    // }
  }
  ////////////////////
  @media screen and (min-width: $break-point-middle) {
  }
</style>
